{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format .",
    "format:write": "biome format . --write",
    "start:database": "cd api && docker compose up --build -d"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "dotenv": "^17.2.3"
  }
}

{
  "private": true,
  "workspaces": [
    "api",
    "web"
  ],

  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "format": "biome format .",
    "format:write": "biome format . --write"
  },

  "devDependencies": {
    "@biomejs/biome": "^2.3.11"
  }
}
